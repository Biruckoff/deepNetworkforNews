В данном проекте реализована модель нейронной сети.
Сама модель хранится в папке model
Неронная сеть содержит 7 скрытых слоёв, два входных слоя и один выходной слой
На один вход подаётся заголовок новости в векторном представлении, на другой  вектор "настроения" предложения
Для синтаксичекого и морфологического анализа используется библиотека Natasha
Для выявления настроения в заголовке используется библиотека dostoevsky
Эмбеддинг также осуществляется через библиотеку Natasha
Для построения неронной сети используется библиотека keras
В качестве вспомогательных библиотек используется tensorflow, pandas, numpy
Данные берутся из файла train.tsv. Первая тысяча строк используется для проверки работы уже обученной нейросети.
